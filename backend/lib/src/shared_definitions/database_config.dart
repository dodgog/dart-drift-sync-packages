class DatabaseConfig{
  final String clientId;
  final String userId;
  final String userToken;
  final bool isServer;

  const DatabaseConfig(this.clientId, this.userId, this.userToken, this
      .isServer,);
}
